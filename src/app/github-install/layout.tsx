export const metadata = {
  title: "PRism — GitHub Installation",
  description: "Complete your PRism GitHub App installation",
};

export default function GitHubInstallLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
